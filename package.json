{
  "name": "profrio-industrial",
  "version": "1.0.0",
  "description": "Sistema de gestión de formularios ProFrio Industrial",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Static app - no build required'",
    "cap-sync": "mkdir -p www && cp -r index.html dashboard-empleado.html dashboard-jefe.html perfil.html formulario-intervencion.html formulario-materiales.html css js www && npx cap sync",
    "ios": "npm run cap-sync && npx cap run ios",
    "open-ios": "npm run cap-sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
